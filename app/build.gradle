plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace 'com.tecdrop.milliontaps'
    compileSdk {
        version = release(36)
    }

    defaultConfig {
        applicationId "com.tecdrop.milliontaps"
        minSdk 16
        targetSdk 36
        versionCode 5
        versionName "2.2"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
}

dependencies {
}